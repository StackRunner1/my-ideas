Here are the steps (Windows, Anaconda/Miniconda in PATH):

Open a new Command Prompt (or VS Code integrated terminal).

(Optional) Update conda:

Create the environment (names cannot contain spaces, using ideas):
Activate it:
Change to your project backend folder:
Install dependencies from requirements.txt (uses pip inside the env):
Alternative (all via conda, may need conda-forge for some):

Verify:
To deactivate later:

Need anything else (e.g. adding to VS Code interpreter)? Just ask.